# The upstream Open WebUI ref the fork's footprint is measured against.
# One ref per line; the first non-comment line wins.
#
# Bumping this is a visible commit on purpose. It changes what
# `check_upstream_footprint.py` considers "upstream", so it is the moment to
# re-read the seam list rather than a detail to slip into an unrelated change.
#
# Fetched in CI with:
#   git fetch --depth 1 https://github.com/open-webui/open-webui.git \
#       refs/tags/<ref>:refs/tags/upstream-<ref>
v0.11.1
